global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "taskflow-api"
    metrics_path: "/api/actuator/prometheus"
    static_configs:
      - targets:
          - "host.docker.internal:8080"

storage:
  tsdb:
    retention:
      time: 30d
